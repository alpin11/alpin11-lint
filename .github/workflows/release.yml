name: release package
on:
  push:
    branches:
      - main

jobs:
  release-package:
    runs-on: self-hosted

    steps:
    - uses: actions/checkout@v2
    - uses: bahmutov/npm-install@v1

    - run: |
        git config user.name Dominik Ager
        git config user.email d.ager@alpin11.com
        git pull
        npm version patch
        git push
        npm publish
